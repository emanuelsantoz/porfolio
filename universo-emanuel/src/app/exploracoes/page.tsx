import type { Metadata } from "next";
import { ExternalLink } from "lucide-react";
import { PortfolioLayout } from "@/components/portfolio/PortfolioLayout";
import { explorations } from "@/data/cases";

export const metadata: Metadata = { title: "Explorações", description: "Protótipos, estudos e experimentos com estado declarado.", alternates: { canonical: "/exploracoes" } };
export default function ExplorationsPage() { return <PortfolioLayout><section className="px-6 py-16 lg:px-10 lg:py-24"><div className="mx-auto max-w-5xl"><p className="font-mono text-xs uppercase tracking-[0.22em] text-[#2166c9]">Explorações</p><h1 className="mt-5 font-[family-name:var(--font-display)] text-5xl font-medium leading-[1.02] tracking-[-0.055em] sm:text-6xl">Nem toda ideia precisa fingir que já é um produto.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-slate-600">Estes trabalhos são hipóteses, protótipos e estudos. Eles aparecem aqui com seu estado declarado, até acumularem evidência suficiente para um case principal.</p><div className="mt-14 grid gap-4">{explorations.map((item) => <article key={item.name} className="rounded-[1.5rem] border border-[#07111f]/10 bg-white p-7"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-[#2166c9]">{item.state}</p><h2 className="mt-3 font-[family-name:var(--font-display)] text-3xl font-semibold tracking-[-0.04em]">{item.name}</h2><p className="mt-3 max-w-2xl leading-7 text-slate-600">{item.description}</p></div>{item.href ? <a href={item.href} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-[#2166c9]">Abrir estudo <ExternalLink size={16} /></a> : null}</div></article>)}</div></div></section></PortfolioLayout>; }
